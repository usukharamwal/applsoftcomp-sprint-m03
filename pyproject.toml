[project]
name = "contexteng"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pymupdf",
    "pandas",
    "matplotlib",
    "seaborn",
    "statsmodels",
    "openpyxl",
    "xlrd",
    "requests",
]
